import {REGISTRO_EXITOSO, 
        REGISTRO_ERROR,
        LIMPIAR_ALERTA, 
        LOGIN_EXITOSO,
        LOGIN_ERROR,
        USUARIO_AUTENTICADO,
        CERRAR_SESION
    } from "../../types/index"

export default (state, action) => {
    switch(action.type) {
        
        default:
            return state;
    }
}